{
  "name": "stm32webemu",
  "version": "0.1.0",
  "description": "STM32F103 (ARM Cortex-M3 / Thumb-2) emulator for the browser",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
